export interface ProductsData {
    id: number
    name: string
    price: number
    img: string
    blackFriday: boolean
    discount: number
    rating: number
    storage: number
    color: string
    order: string
}